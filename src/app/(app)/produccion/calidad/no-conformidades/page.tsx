import Link from "next/link"; import { redirect } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { obtenerUsuario } from "@/lib/auth"; import { puedeRealizar } from "@/lib/permisos";
export default async function NoConformidadesPage() { const usuario=await obtenerUsuario(); if(!usuario||!(await puedeRealizar(usuario,"produccion","ver"))) redirect("/"); const filas=await prisma.noConformidadCalidad.findMany({where:{empresaId:usuario.empresaId},include:{controlCalidad:{include:{loteGranel:{include:{formula:{include:{producto:true}}}}}}},orderBy:{creadoEn:"desc"}}); return <div className="max-w-5xl"><div className="flex justify-between"><div><h1 className="text-2xl font-semibold">No conformidades y CAPA</h1><p className="text-neutral-500">Investigación y cierre verificable de desviaciones internas.</p></div><Link href="/produccion/calidad" className="boton-secundario">Volver</Link></div><table className="tabla mt-6"><thead><tr><th>Expediente</th><th>Lote / producto</th><th>Estado</th><th>Responsable</th><th>Compromiso</th></tr></thead><tbody>{filas.map(n=><tr key={n.id}><td><Link className="font-mono text-xs hover:underline" href={`/produccion/calidad/no-conformidades/${n.id}`}>NC-{n.id.slice(-8).toUpperCase()}</Link></td><td>{n.controlCalidad.loteGranel.codigo}<span className="block text-xs text-neutral-500">{n.controlCalidad.loteGranel.formula.producto.nombre}</span></td><td>{n.estado.replaceAll("_"," ")}</td><td>{n.responsableNombre??"Sin asignar"}</td><td>{n.fechaCompromiso?new Intl.DateTimeFormat("es-PE").format(n.fechaCompromiso):"—"}</td></tr>)}</tbody></table></div>; }
