import { redirect } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { obtenerUsuario } from "@/lib/auth";
import { puedeRealizar } from "@/lib/permisos";
import NivelFormulario from "./NivelFormulario";
import { desactivarNivelCompra } from "./actions";
import { obtenerEmpresaActivaId } from "@/lib/empresas";
export default async function AprobacionesComprasPage() { const usuario = await obtenerUsuario(); if (!usuario || usuario.rol !== "ADMIN" || !(await puedeRealizar(usuario, "configuracion", "ver"))) redirect("/"); const empresaId = await obtenerEmpresaActivaId(); const [niveles, plantas] = await Promise.all([prisma.nivelAprobacionCompra.findMany({ where: { empresaId, activo: true }, include: { almacen: { select: { nombre: true } } }, orderBy: { orden: "asc" } }), prisma.almacen.findMany({ where: { empresaId, tipo: "PLANTA", activo: true }, select: { id: true, nombre: true }, orderBy: { nombre: "asc" } })]); return <div className="max-w-4xl"><h1 className="text-2xl font-semibold">Aprobaciones de compras</h1><p className="text-neutral-500 mt-1 mb-6">Cada orden recorre, en secuencia, todos los niveles cuyo umbral haya alcanzado. Un nivel acotado a una planta se <strong>suma</strong> a los generales para las órdenes destinadas a esa planta: configurar una planta agrega controles, nunca los quita.</p><NivelFormulario plantas={plantas}/><table className="tabla mt-6"><thead><tr><th>Orden</th><th>Nivel</th><th>Desde</th><th>Rol</th><th>Alcance</th><th></th></tr></thead><tbody>{niveles.map((n) => <tr key={n.id}><td>{n.orden}</td><td>{n.nombre}</td><td>PEN {n.montoDesdePen.toNumber().toFixed(2)}</td><td>{n.rolAprobador}</td><td>{n.almacen?.nombre ?? "Toda la compañía"}</td><td><form action={desactivarNivelCompra.bind(null,n.id)}><button className="text-red-600 hover:underline">Desactivar</button></form></td></tr>)}</tbody></table></div>; }
