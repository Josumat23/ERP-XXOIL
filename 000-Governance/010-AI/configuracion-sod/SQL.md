# SQL — Modelo de datos — configuracion-sod

Sin cambios de schema ni migraciones. Se reutilizan `GrupoSeguridad`, `PermisoGrupo` y la auditoría de maestros existente. La matriz es lógica de dominio versionada en `src/lib/segregacionFunciones.ts`.
