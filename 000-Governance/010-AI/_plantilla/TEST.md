# TEST — Verificación — &lt;módulo&gt;

Registro de lo probado en navegador (no solo `tsc --noEmit`) antes de pedir confirmación de push.

## Escenario 1 — &lt;nombre&gt;
- **Datos usados:** &lt;reales del seed, o sintéticos insertados por SQL y removidos después&gt;
- **Pasos:** &lt;qué se hizo en el navegador&gt;
- **Resultado esperado:** &lt;...&gt;
- **Resultado obtenido:** &lt;...&gt;

## `tsc --noEmit`
- Baseline conocido: 6 archivos preexistentes (ver `000-Governance/010-AI/README.md` o la memoria del proyecto). Confirmar que no aparecieron archivos nuevos en la lista.

## Datos de prueba a limpiar
- &lt;qué se insertó directo por SQL y ya se removió&gt;
